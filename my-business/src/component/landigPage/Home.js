import React from 'react';
import Header from '../../common/Header';

const HomePage = (props)=>{
    
    return(
        <div>
          <Header {...props}/>
          Hello
        </div>
    )
}

export default HomePage;